/*******************************************************************************
Code generated by platkmframework 
*******************************************************************************/


import { useEffect, useState} from "react"

import { useSearch } from "../../components/hooks/useSearch";
import { Search } from "../../components/search/pages/Search";
import { useMessage } from "../../components/hooks/useMessage";
import { ConfirmDialog } from "../../components/dialog/ConfirmDialog";
import { useFetch } from "../../components/hooks/useFetch";
import { Message } from "../../components/message/Message";
import { converToLabel } from "../../js/util";

export const ProductsListPage = ({action, setAction})  => {

    // Control de ordenación
    const  {clean, success, error, msg} = useMessage();
    const [openDelConfirm, setOpenDelConfirm] = useState({value:'', index:'', open:false})

    const useSearchHook = useSearch({url:'products/criteria', error});
    const {search, result, handleSort, filter} = useSearchHook;
    const {post, put, get, del} = useFetch();

    useEffect(()=>{ 
        search();
    }, [])

    useEffect(()=>{
        if(action.value == "search"){
            search();
        }
    }, [action])


    const handleYes = async( event)=>{
        event.preventDefault();  
        clean();
     
        del({url:'products/' + openDelConfirm.value },
			(status, data)=>{ 
				success('record removed')
				search()
                setAction({...action, value:'cleanForm', id:openDelConfirm.value})
			}, (status, msg)=>{
				error(msg)
			} 
		)  
    }

    const handleDelete = async (event, id) => {
        event.preventDefault();  
        setOpenDelConfirm({...openDelConfirm, value:id, open:true});
    }

    const handleEdit = async (event, id) => {
        event.preventDefault();  
        setAction({...action, value:'formEdit', id:id})
    }
 

    return (
        <>
        <div className="container">
            <div className="card">
                <div><Message msg={msg}/></div> 
                <h2 className="card-title"> Products List </h2> 
                <Search useSearch={useSearchHook}>
                    <table className="table table-striped table-hover">
                        <thead className="thead-light">
                            <tr>
   <th scope="col" onClick={() => handleSort("name")} style={{ cursor: "pointer" }}>{converToLabel('name')}
                                    {
                                        filter.order === "name" && (
                                            <span className="sort-indicator">
                                                {filter.orderType === "asc" ? "▲" : "▼"}
                                            </span>
                                        )
                                    }    
                                </th> 
   <th scope="col" onClick={() => handleSort("description")} style={{ cursor: "pointer" }}>{converToLabel('description')}
                                    {
                                        filter.order === "description" && (
                                            <span className="sort-indicator">
                                                {filter.orderType === "asc" ? "▲" : "▼"}
                                            </span>
                                        )
                                    }    
                                </th> 
   <th scope="col" onClick={() => handleSort("categoryId")} style={{ cursor: "pointer" }}>{converToLabel('category_id')}
                                    {
                                        filter.order === "category_id" && (
                                            <span className="sort-indicator">
                                                {filter.orderType === "asc" ? "▲" : "▼"}
                                            </span>
                                        )
                                    }    
                                </th> 
   <th scope="col" onClick={() => handleSort("purchasePrice")} style={{ cursor: "pointer" }}>{converToLabel('purchase_price')}
                                    {
                                        filter.order === "purchase_price" && (
                                            <span className="sort-indicator">
                                                {filter.orderType === "asc" ? "▲" : "▼"}
                                            </span>
                                        )
                                    }    
                                </th> 
   <th scope="col" onClick={() => handleSort("salePrice")} style={{ cursor: "pointer" }}>{converToLabel('sale_price')}
                                    {
                                        filter.order === "sale_price" && (
                                            <span className="sort-indicator">
                                                {filter.orderType === "asc" ? "▲" : "▼"}
                                            </span>
                                        )
                                    }    
                                </th> 
   <th scope="col" onClick={() => handleSort("unit")} style={{ cursor: "pointer" }}>{converToLabel('unit')}
                                    {
                                        filter.order === "unit" && (
                                            <span className="sort-indicator">
                                                {filter.orderType === "asc" ? "▲" : "▼"}
                                            </span>
                                        )
                                    }    
                                </th> 
   <th scope="col" onClick={() => handleSort("minQuantity")} style={{ cursor: "pointer" }}>{converToLabel('min_quantity')}
                                    {
                                        filter.order === "min_quantity" && (
                                            <span className="sort-indicator">
                                                {filter.orderType === "asc" ? "▲" : "▼"}
                                            </span>
                                        )
                                    }    
                                </th> 
   <th scope="col" onClick={() => handleSort("batchManaged")} style={{ cursor: "pointer" }}>{converToLabel('batch_managed')}
                                    {
                                        filter.order === "batch_managed" && (
                                            <span className="sort-indicator">
                                                {filter.orderType === "asc" ? "▲" : "▼"}
                                            </span>
                                        )
                                    }    
                                </th> 
   <th scope="col" onClick={() => handleSort("perishable")} style={{ cursor: "pointer" }}>{converToLabel('perishable')}
                                    {
                                        filter.order === "perishable" && (
                                            <span className="sort-indicator">
                                                {filter.orderType === "asc" ? "▲" : "▼"}
                                            </span>
                                        )
                                    }    
                                </th> 
   <th scope="col" onClick={() => handleSort("updatedDate")} style={{ cursor: "pointer" }}>{converToLabel('updated_date')}
                                    {
                                        filter.order === "updated_date" && (
                                            <span className="sort-indicator">
                                                {filter.orderType === "asc" ? "▲" : "▼"}
                                            </span>
                                        )
                                    }    
                                </th> 
                                <th scope="col"></th> 
                            </tr>
                        </thead>
                        <tbody>
                        {
                            result.recordList?.map((row, i) =>(
				<tr key={row.productId  + "_" + i}>
					<td>{row.name} </td> 
					<td>{row.description} </td> 
					<td>{row.categoryId} </td> 
					<td>{row.purchasePrice} </td> 
					<td>{row.salePrice} </td> 
					<td>{row.unit} </td> 
					<td>{row.minQuantity} </td> 
					<td>{row.batchManaged} </td> 
					<td>{row.perishable} </td> 
					<td>{row.updatedDate} </td> 
                                    			<td>
                                        				<button className="btn" onClick={(event) => handleEdit(event, row.productId)}>Edit</button> &nbsp;
                                        				<button  className="btn btn-danger" onClick={(event) => handleDelete(event, row.productId)}>Remove</button>
                                    			</td>
                                		</tr> 
                            ))
                        }
                        </tbody>
                    </table>
                </Search>   
            </div>
        </div> 
        <ConfirmDialog openDelConfirm = {openDelConfirm} handleYes={handleYes} />       
        </>
    )
}